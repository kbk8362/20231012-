# include <stdio.h>
# include <stdlib.h>
#define MAX_ELEMENT 200
using System.Reflection.Metadata;
typedef struct {
    int key;
} element;
typedef struct {
    element heap[MAX_ELEMENT];
    int heap_size;
} HeapType;